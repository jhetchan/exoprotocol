{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,subprocess; d=json.load(sys.stdin); cmd=d.get('input',{}).get('command',''); sys.exit(subprocess.run(['exo','check','--format','json']).returncode) if 'git commit' in cmd or 'git push' in cmd else None\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
